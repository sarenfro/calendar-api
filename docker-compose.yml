version: '3.8'

services:
  calendar-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      # Mount for persistent data (optional)
      - calendar-data:/app/data

volumes:
  calendar-data:
